# Comments Folder

This folder will contain user comments, with each comment saved as a separate text file.

Comments are automatically saved here when users submit them through the website.

Each comment file will be named: `comment_[timestamp]_[random].txt`
